import { TypeUtil } from "../Utility/TypeUtil.js";
export class UriUtility {
    static parseUrls(urls) {
        return (TypeUtil.isArray(urls) ? urls : [urls])
            .reduce((accumulator, iteratee) => accumulator.concat((TypeUtil.isArray(iteratee) ? iteratee
            : iteratee.split(/[,;]/).map((url) => url.trim())
                .filter((url) => !!url))), []);
    }
    static isSecure(url) {
        return 0 === url.toLowerCase().indexOf("https");
    }
}
//# sourceMappingURL=UriUtility.js.map