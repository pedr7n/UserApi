import { throwError } from "../Exceptions/index.js";
export function isValidUri(uriString) {
    try {
        new URL(uriString);
        return true;
    }
    catch {
        return false;
    }
}
export function validateUri(uriString) {
    if (!isValidUri(uriString)) {
        throwError("InvalidArgumentException", `Uri ${uriString} is invalid.`);
    }
}
//# sourceMappingURL=UriUtil.js.map