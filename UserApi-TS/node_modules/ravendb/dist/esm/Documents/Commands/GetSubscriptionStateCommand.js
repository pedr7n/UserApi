import { RavenCommand } from "../../Http/RavenCommand.js";
export class GetSubscriptionStateCommand extends RavenCommand {
    _subscriptionName;
    constructor(subscriptionName) {
        super();
        this._subscriptionName = subscriptionName;
    }
    createRequest(node) {
        const uri = node.url + "/databases/" + node.database + "/subscriptions/state?name=" + this._urlEncode(this._subscriptionName);
        return {
            uri
        };
    }
    async setResponseAsync(bodyStream, fromCache) {
        return this._parseResponseDefaultAsync(bodyStream);
    }
    get isReadRequest() {
        return true;
    }
}
//# sourceMappingURL=GetSubscriptionStateCommand.js.map