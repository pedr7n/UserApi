import { MultiGetCommand } from "../../Commands/MultiGet/MultiGetCommand.js";
export class MultiGetOperation {
    _session;
    constructor(session) {
        this._session = session;
    }
    createRequest(requests) {
        return new MultiGetCommand(this._session.requestExecutor, this._session.conventions, requests, this._session.sessionInfo);
    }
    setResult(result) {
        // empty
    }
}
//# sourceMappingURL=MultiGetOperation.js.map