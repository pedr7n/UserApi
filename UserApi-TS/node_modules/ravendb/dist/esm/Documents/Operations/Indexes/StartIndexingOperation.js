import { RavenCommand } from "../../../Http/RavenCommand.js";
export class StartIndexingOperation {
    get resultType() {
        return "CommandResult";
    }
    getCommand(conventions) {
        return new StartIndexingCommand();
    }
}
export class StartIndexingCommand extends RavenCommand {
    get isReadRequest() {
        return false;
    }
    createRequest(node) {
        const uri = node.url + "/databases/" + node.database + "/admin/indexes/start";
        return {
            method: "POST",
            uri
        };
    }
}
//# sourceMappingURL=StartIndexingOperation.js.map