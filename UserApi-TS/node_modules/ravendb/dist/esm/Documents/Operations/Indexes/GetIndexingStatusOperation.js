import { RavenCommand } from "../../../Http/RavenCommand.js";
export class GetIndexingStatusOperation {
    get resultType() {
        return "CommandResult";
    }
    getCommand(conventions) {
        return new GetIndexingStatusCommand();
    }
}
export class GetIndexingStatusCommand extends RavenCommand {
    createRequest(node) {
        const uri = node.url + "/databases/" + node.database + "/indexes/status";
        return { uri };
    }
    async setResponseAsync(bodyStream, fromCache) {
        if (!bodyStream) {
            this._throwInvalidResponse();
        }
        return this._parseResponseDefaultAsync(bodyStream);
    }
    get isReadRequest() {
        return true;
    }
}
//# sourceMappingURL=GetIndexingStatusOperation.js.map