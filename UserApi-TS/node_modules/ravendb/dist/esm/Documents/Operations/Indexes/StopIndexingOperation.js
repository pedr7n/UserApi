import { RavenCommand } from "../../../Http/RavenCommand.js";
export class StopIndexingOperation {
    get resultType() {
        return "CommandResult";
    }
    getCommand(conventions) {
        return new StopIndexingCommand();
    }
}
export class StopIndexingCommand extends RavenCommand {
    get isReadRequest() {
        return false;
    }
    createRequest(node) {
        const uri = node.url + "/databases/" + node.database + "/admin/indexes/stop";
        return {
            method: "POST",
            uri
        };
    }
}
//# sourceMappingURL=StopIndexingOperation.js.map