import { SpatialCriteria } from "./SpatialCriteria.js";
import { ShapeToken } from "../../Session/Tokens/ShapeToken.js";
export class CircleCriteria extends SpatialCriteria {
    _radius;
    _latitude;
    _longitude;
    _radiusUnits;
    constructor(radius, latitude, longitude, radiusUnits, relation, distErrorPercent) {
        super(relation, distErrorPercent);
        this._radius = radius;
        this._latitude = latitude;
        this._longitude = longitude;
        this._radiusUnits = radiusUnits;
    }
    _getShapeToken(addQueryParameter) {
        return ShapeToken.circle(addQueryParameter(this._radius), addQueryParameter(this._latitude), addQueryParameter(this._longitude), this._radiusUnits);
    }
}
//# sourceMappingURL=CircleCriteria.js.map