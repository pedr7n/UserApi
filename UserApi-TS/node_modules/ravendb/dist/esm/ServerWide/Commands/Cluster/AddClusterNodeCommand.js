import { RavenCommand } from "../../../Http/RavenCommand.js";
import { RaftIdGenerator } from "../../../Utility/RaftIdGenerator.js";
export class AddClusterNodeCommand extends RavenCommand {
    _url;
    _tag;
    _watcher;
    constructor(url, tag, watcher = false) {
        super();
        this._url = url;
        this._tag = tag;
        this._watcher = watcher;
    }
    get isReadRequest() {
        return false;
    }
    createRequest(node) {
        let uri = node.url + "/admin/cluster/node?url=" + this._urlEncode(this._url) + "&watcher=" + this._watcher;
        if (this._tag) {
            uri += "&tag=" + this._tag;
        }
        return {
            uri,
            method: "Put"
        };
    }
    getRaftUniqueRequestId() {
        return RaftIdGenerator.newId();
    }
}
//# sourceMappingURL=AddClusterNodeCommand.js.map