import { throwError } from "../../../Exceptions/index.js";
import { RavenCommand } from "../../../Http/RavenCommand.js";
import { RaftIdGenerator } from "../../../Utility/RaftIdGenerator.js";
export class DeleteServerWideAnalyzerOperation {
    _analyzerName;
    constructor(analyzerName) {
        if (!analyzerName) {
            throwError("InvalidArgumentException", "AnalyzerName cannot be null");
        }
        this._analyzerName = analyzerName;
    }
    get resultType() {
        return "CommandResult";
    }
    getCommand(conventions) {
        return new DeleteServerWideAnalyzerCommand(this._analyzerName);
    }
}
class DeleteServerWideAnalyzerCommand extends RavenCommand {
    _analyzerName;
    constructor(analyzerName) {
        super();
        if (!analyzerName) {
            throwError("InvalidArgumentException", "AnalyzerName cannot be null");
        }
        this._analyzerName = analyzerName;
    }
    createRequest(node) {
        const uri = node.url + "/admin/analyzers?name=" + this._urlEncode(this._analyzerName);
        return {
            uri,
            method: "DELETE"
        };
    }
    get isReadRequest() {
        return false;
    }
    getRaftUniqueRequestId() {
        return RaftIdGenerator.newId();
    }
}
//# sourceMappingURL=DeleteServerWideAnalyzerOperation.js.map