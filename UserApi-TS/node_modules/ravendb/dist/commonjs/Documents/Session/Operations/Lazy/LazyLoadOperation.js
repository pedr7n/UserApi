"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.LazyLoadOperation = void 0;
const LoadOperation_js_1 = require("../LoadOperation.js");
const GetRequest_js_1 = require("../../../Commands/MultiGet/GetRequest.js");
const GetDocumentsCommand_js_1 = require("../../../Commands/GetDocumentsCommand.js");
const StreamUtil_js_1 = require("../../../../Utility/StreamUtil.js");
const StringUtil_js_1 = require("../../../../Utility/StringUtil.js");
const StringBuilder_js_1 = require("../../../../Utility/StringBuilder.js");
class LazyLoadOperation {
    _clazz;
    _session;
    _loadOperation;
    _ids;
    _includes;
    _alreadyInSession = [];
    _result;
    _queryResult;
    _requiresRetry;
    constructor(session, loadOperation, clazz) {
        this._clazz = clazz;
        this._session = session;
        this._loadOperation = loadOperation;
    }
    createRequest() {
        const queryBuilder = new StringBuilder_js_1.StringBuilder("?");
        if (this._includes) {
            for (const include of this._includes) {
                queryBuilder.append("&include=").append(include);
            }
        }
        let hasItems = false;
        for (const id of this._ids) {
            if (this._session.isLoadedOrDeleted(id)) {
                this._alreadyInSession.push(id);
            }
            else {
                hasItems = true;
                queryBuilder.append("&id=")
                    .append(encodeURIComponent(id));
            }
        }
        if (!hasItems) {
            // no need to hit the server
            this._result = this._loadOperation.getDocuments(this._clazz);
            return null;
        }
        const getRequest = new GetRequest_js_1.GetRequest();
        getRequest.url = "/docs";
        getRequest.query = queryBuilder.toString();
        return getRequest;
    }
    byId(id) {
        if (StringUtil_js_1.StringUtil.isNullOrEmpty(id)) {
            return this;
        }
        if (!this._ids) {
            this._ids = [id];
        }
        return this;
    }
    byIds(ids) {
        this._ids = Array.from(new Set(ids.filter(x => !StringUtil_js_1.StringUtil.isNullOrEmpty(x))));
        return this;
    }
    withIncludes(includes) {
        this._includes = includes;
        return this;
    }
    get result() {
        return this._result;
    }
    set result(result) {
        this._result = result;
    }
    get queryResult() {
        return this._queryResult;
    }
    set queryResult(queryResult) {
        this._queryResult = queryResult;
    }
    get requiresRetry() {
        return this._requiresRetry;
    }
    set requiresRetry(result) {
        this._requiresRetry = result;
    }
    async handleResponseAsync(response) {
        if (response.forceRetry) {
            this.result = null;
            this.requiresRetry = true;
            return;
        }
        const multiLoadResult = await GetDocumentsCommand_js_1.GetDocumentsCommand.parseDocumentsResultResponseAsync((0, StreamUtil_js_1.stringToReadable)(response.result), this._session.conventions);
        this._handleResponse(multiLoadResult);
    }
    _handleResponse(loadResult) {
        if (this._alreadyInSession.length) {
            // push this to the session
            new LoadOperation_js_1.LoadOperation(this._session)
                .byIds(this._alreadyInSession)
                .getDocuments(this._clazz);
        }
        this._loadOperation.setResult(loadResult);
        if (!this._requiresRetry) {
            this._result = this._loadOperation.getDocuments(this._clazz);
        }
    }
    getResult() {
        return null;
    }
}
exports.LazyLoadOperation = LazyLoadOperation;
//# sourceMappingURL=LazyLoadOperation.js.map